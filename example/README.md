# Example deck

From the project root:

```bash
npm install
npm run dev
```

Slide images are prepared automatically after Slidev starts.

Add `OPENAI_API_KEY` to `example/.env`, or export it in the shell before starting
Slidev. Open the presenter view to see the Speech nav control.
